#include <stdio.h>
#include <stdlib.h>
int i,k,*p0=&k,*p1=&i;
void run(int *a)
{
  int j;
  for(i=0;i<80;i++)
  {
  k=80-i;
  for(j=*a;j<80;j++)
  printf(" ");
  printf("hhy");
  system("cls");
  }	
}
int main(void)
{
  for(;;)
  {
  run(p0);
  run(p1);
  }
  getchar();
  return 0;
}
