#include<time.h>
#include<stdio.h>
#include<stdlib.h>
#define N 100000
#define random(x) (rand()%(x))
int n;
int main(){
	srand((int)time(0));
	freopen("sig","r",stdin);
	scanf("%d",&n);
	printf("n=%d",n+=100);
	freopen("a.in","w",stdout);
	printf("%d\n",n);
	for (int i=1;i<=n;i++)printf("%d ",random(N));
	return 0;
}
